/** @param {NS} ns */
export async function main(ns) {
	// ports are so you can share data between scripts without storing them in a file, etc
	// write to a port, then read from it (pops/consumes value)
	// var text = ns.args.join(' ');
	// for (var i = 1; i <= 20; ++i) {
	// 	var res = await ns.writePort(parseInt(i), text);
	// 	ns.tprint(res);
	// 	await ns.sleep(100);
	// }

	// var corpName = ns.args.length > 0 ? ns.args[0] : 'big-bois';
	// ns.tprint(`Corp '${corpName}' created: ${ns.corporation.createCorporation(corpName, true)}`);

	
}